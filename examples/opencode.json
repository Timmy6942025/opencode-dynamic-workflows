{
  "$schema": "https://opencode.ai/config.json",
  "plugin": ["oc-dw"],
  "agent": {
    "dynamic-verifier": {
      "description": "Read-only verifier for dynamic workflow outputs",
      "mode": "subagent",
      "permission": {
        "edit": "deny",
        "bash": "ask"
      }
    }
  }
}
